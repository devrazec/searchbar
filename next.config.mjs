/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/searchbar',
  images: {
    unoptimized: true,
  },
  reactStrictMode: false,
};

export default nextConfig;
